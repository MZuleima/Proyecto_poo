# Revisión del proyecto — Zule (Gestor de Incidencias)

**Fuente de verdad:** `Gestor_incidencias/04_Sqlite/`
**Fecha de revisión:** 2026-05-27
**Revisión anterior:** `REVIEW_2026_04_22.md` (fases 01-03)

## REVISIÓN FASE 04 - 2026-05-27 — Nota: 1/10

La fase 04 no está implementada. La carpeta `04_Sqlite/` existe y replica la estructura de `03_Testing/`, pero:

- **`crear_bd.py` está vacío.** No crea ninguna tabla ni inserta datos.
- **No hay repositorio SQLite.** En `infrastructure/` solo hay `datos_iniciales.py`, que define `RepositorioMemoria` (la misma implementación de la fase 03). No existe `repositorio_sqlite.py` ni equivalente.
- **No hay esquema SQL.** El proyecto sigue funcionando contra memoria.
- **`CHANGELOG.md` no tiene entrada de fase 04.** Sigue con `[2.0.0]` (fase 02) y `[3.0.0]` (fase 03), sin nada del trabajo de SQLite.
- **Falta `docs/DISEÑO_BD.md`.**

### Pasos para arrancar la fase 04

1. Rellenar `crear_bd.py` con el esquema: una tabla para incidencias (con `id_incidencia`, `descripcion`, `estado`, `resolucion`, `fecha_apertura`, `fecha_cierre` y claves foráneas a técnico y sala), una tabla para técnicos y otra para salas. Insertar los datos iniciales que ahora viven en `datos_iniciales.py`.
2. Crear `infrastructure/repositorio_sqlite.py` con una clase `RepositorioIncidenciasSQLite` que herede del contrato de dominio y use consultas parametrizadas con `?`.
3. Cambiar el menú para que use el repositorio SQLite en lugar del de memoria.
4. Añadir `docs/DISEÑO_BD.md` con la descripción del esquema.
5. Añadir entrada `[0.4.0] - YYYY-MM-DD` al `CHANGELOG.md` (de paso, corregir el versionado de `[2.0.0]`/`[3.0.0]` a `[0.2.0]`/`[0.3.0]` como se indicó en la revisión del 22-04).

## REVISIÓN FASE 05 - 2026-05-27 — Nota: 0/10

No hay entrega de fase 05. No existe `presentation/app.py` ni carpeta de plantillas, y la fase 04 sigue sin completarse.

### Lo que falta para llegar al objetivo (subfase 04 con 2 formularios)

Conviene cerrar primero la fase 04 (SQLite) antes de empezar la fase 05. Una vez cerrada:

- **Subfase 01 (Flask básico):** crear `presentation/app.py` con `Flask` y exponer las operaciones del servicio (`nueva_incidencia`, `obtener_listado`, `resolver`, etc.) como rutas. Las de escritura redirigen tras actuar; las excepciones de dominio se traducen a códigos HTTP (404, 409, 400).
- **Subfase 02 (Observabilidad):** añadir manejadores `@app.errorhandler(404)` y `(500)`, una ruta `/ayuda` con `app.url_map.iter_rules()`, y un `@app.before_request` con logging a `.log`. Añadir `*.log` al `.gitignore`.
- **Subfase 03 (Plantillas Jinja2):** crear `presentation/templates/base.html` y una plantilla hija por cada ruta de lectura (listado de incidencias, detalle, avisos por técnico, etc.). Usar `render_template()`.
- **Subfase 04 (Formularios POST):** al menos 2 operaciones de escritura (por ejemplo, registrar incidencia y resolver incidencia) con plantilla de formulario y ruta con `methods=['GET', 'POST']`. Post/Redirect/Get tras éxito; re-render con error si el POST falla.
